/*
3. Tabuada personalizada
Peça um número e mostre a tabuada desse
número (de 1 a 10) usando um for.
*/

let numero = 10
for (let i = 1; i <= 10; i++) {
    let resultado = numero * i;
    console.log(`${numero} x ${i} = ${resultado}`);
}